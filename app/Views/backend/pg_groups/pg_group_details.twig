{% include "backend/header.twig" %}
{% include "backend/navbar.twig" %}
<div class="container"></div>
<div class="container blogs">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex"><h1>Proje Grubu Düzenleme Sayfası</h1>
                <form class="deletebutton" method = "post" action="{{ base_url("management/pg_groups") }}">
                    <input name="pg_group_delete_id" type="hidden" value="{{ pg_group['pg_group_id'] }}">
                    <button type="submit"><span class="material-symbols-outlined">
delete
</span></button>
                </form></div>
            <form class="form" enctype="multipart/form-data" method = "post" action="{{ base_url("management/pg_groups") }}">
                <div>
                    <input name="pg_group_edit_id" type="hidden" value="{{ pg_group['pg_group_id'] }}">
                    <label for="">Proje Grubu Adı:</label>
                    <div><input name="pg_group_edit_title" type="text" value="{{ pg_group['pg_group_title'] }}"></div>
                </div>
                <div style="margin-top: 1%;">
                    <label for="">Proje Grubu Açıklaması:</label>
                    <div><input name="pg_group_edit_description" type="text" value="{{ pg_group['pg_group_description'] }}"></div>
                </div>
                <div style="margin-top: 1%;">
                    <label for="">Proje Grubu Resmi:</label>
                    <div><input name="pg_group_edit_image" type="file"></div>
                </div>

                <button style="margin-top: 1%; margin-left: 0;" type="submit">Düzenle</button>
            </form>

        </div>
        <div class="col-md-2" style="padding: 1% 0.9%;">
        </div>
    </div>

</div>
{% include "backend/footer.twig" %}