{% include "backend/header.twig" %}
{% include "backend/navbar.twig" %}
<div class="container"></div>
<div class="container blogs">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex"><h1>Referans Düzenleme Sayfası</h1>
                <form class="deletebutton" method = "post" action="{{ base_url("management/references") }}">
                    <input name="reference_delete_id" type="hidden" value="{{ reference['reference_id'] }}">
                    <button type="submit"><span class="material-symbols-outlined">
delete
</span></button>
                </form></div>
            <form class="form" method = "post" enctype="multipart/form-data" action="{{ base_url("management/references") }}">
                <div>
                    <input name="reference_edit_id" type="hidden" value="{{ reference['reference_id'] }}">
                    <label for="">Referans Adı:</label>
                    <div><input name="reference_edit_title" type="text" value="{{ reference['reference_title'] }}"></div>
                </div>
                <div style="margin-top: 1%;">
                    <label for="">Referans Açıklaması:</label>
                    <div><input name="reference_edit_description" type="text" value="{{ reference['reference_description'] }}"></div>
                </div>
                <div style="margin-top: 1%;">
                    <label for="">Referans Resmi:</label>
                    <div><input name="reference_edit_image" type="file"></div>
                </div>

                <button style="margin-top: 1%; margin-left: 0;" type="submit">Düzenle</button>
            </form>

        </div>
        <div class="col-md-2" style="padding: 1% 0.9%;">
        </div>
    </div>

</div>
{% include "backend/footer.twig" %}